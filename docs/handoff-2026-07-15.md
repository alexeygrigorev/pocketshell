# Orchestrator session handoff — 2026-07-15

`main` @ **086e1dda** at handoff (clean, 0 open PRs). This doc lets another
orchestrator take over. Read `process.md` first; this is the live state on top
of it. Live tracking issue: **#1623**.

## Continuation update — recovered unfinished work

`main` advanced to **08a2a75f** with the locked Codex-subagent rule documented
in `process.md`: backlog agents must be **`gpt-5.6-sol` High**, never an external
shell-launched agent. The persistent defaults are already set in
`~/.codex/config.toml`; start a new task/restart Codex so the current parent's
Medium live override no longer propagates to children. Verify the first child
before it edits.

Both handoff agents had stopped but their uncommitted worktrees were recovered:

- **#1615** — `.claude/worktrees/agent-a9a4e25adf907281c` has one modified test
  file. Round 2 removed the flaky payload 50× stress guard, retains the payload
  single-shot seam, and keeps the proven agent-variant 50× guard. Recovery ran
  the two single-shot reconnect tests plus the agent stress guard sequentially:
  **3/3 passed** (`BUILD SUCCESSFUL`). It still needs a `gpt-5.6-sol` High
  implementer completion comment, then an independent High reviewer.
- **#1616 PR-1** — `.claude/worktrees/agent-a2c4649cfea0a1b37` has three modified
  production files plus untracked
  `PromptComposerDraftLossOnFinalizeTest.kt`. Recovery ran the new class:
  **10/11 passed**; `restoreFailedCrossTargetDoesNotStompOtherSessionPersistedDraft`
  failed at line 408. This is unfinished implementation/test work, not approval
  evidence. A High implementer must diagnose it without weakening assertions,
  finish the full gate, and post status before review.

Do not discard or recreate either worktree. Resume those two streams first.

### Continuation update — fresh #1610 recurrence and agent-launch blocker

The maintainer reported that PocketShell sessions running Codex again make the
connection unstable while Termius remains stable. The latest mirrored device
log proves a PocketShell control-channel failure chain rather than a general SSH
or host outage:

- `2026-07-15T18:28:59Z`: generation 60's tmux `-CC` reader failed with
  `exceptionClass=TransportException`, `disconnectSource=read_failure`, and
  `transportDropSource=control_channel`; PocketShell immediately took the
  shared `lease_transport` down.
- `2026-07-15T18:30:19Z` through `18:30:49Z`: at least 14 subsequent
  `pane_input_send` failures reported `reader_eof` on that same generation
  while its surfaced status still said `Connected`.
- `2026-07-15T18:31:15Z`: foreground healing observed the client disconnected
  and cycled the lease again.

The Termius comparison is consistent with #1610: an ordinary shell client does
not exercise PocketShell's per-session tmux `-CC` control channel and pane-input
delivery lifecycle. Step 2 remains a maintainer rewrite-vs-containment decision;
do not implement it implicitly.

Delegated work is also paused by a verified Codex launcher mismatch. The parent
session, global config, and an exactly named custom profile all specify
`gpt-5.6-sol` High, but the spawned child's own turn metadata still recorded
`gpt-5.6-sol` Medium. The child was interrupted before analysis. The current
`spawn_agent` interface exposes no model/effort argument, so do not resume the
backlog with Medium agents or shell-launched substitutes; only redispatch after
a child records High.

---

## 1. In-flight agents (CHECK THESE FIRST — they may have finished)

Two implementer agents were running at handoff. Check the issue for their status
comment + the worktree for the diff, then run the normal review → gate → merge
loop.

| Work | Issue | Worktree | State |
|------|-------|----------|-------|
| **Test-flake fix (rescoped)** | #1615 | `.claude/worktrees/agent-a9a4e25adf907281c` | Round-2 implementer running. Landing ONLY the agent-variant teardown-seam fix (fixes the #1614 flake); the flaky payload-variant 50× guard must be pulled from CI; the payload-variant real root is split to **#1617**. Prior reviewer round: CHANGES REQUESTED (payload guard flaked at 50×). |
| **Composer draft-loss (PR-1)** | #1616 (PR-1) | `agent-a2c4649cfea0a1b37` (isolation worktree) | Implementer running. The data-loss BUG: `markSendDelivered` (VM:~1063) unconditionally clears the post-handoff draft + `onDelivered` closes the sheet unconditionally (`TmuxSessionScreen.kt:1022-1025`). Fix = never clear/close over a new draft. Class coverage mandated. |

**Next action for each:** when the agent's status comment lands, dispatch a
`reviewer` (D31/D32/D33 gates), then integrate in a clean `.worktrees/issue-<N>`
worktree, run the gate (incl. `check-file-size-hygiene.sh` +
`check-connection-vm-ratchet.sh` for VM touches), PR → merge on green required
checks (`Unit tests`, `Python utility tests`). Use an `oncall-engineer` +
`scripts/watch-ci.py` to watch — never poll CI yourself.

---

## 2. Shipped this session (11 merged & closed) — for context

#1584 (composer TUI classification) · #1570 (Codex idle detection) · #1581 (CI
NDK retry) · #1529 (exactly-once needle) · #1566 (usage pill attribution) ·
#1565 (reset weekday) · #1564 (Codex usage mislabel + **quse 0.0.11 published to
PyPI**) · #1602 (clogged queue) · #1603 (conv-load auto-retry) · #1610-Step-1
(zero-leak diagnostics) · #1613 (offline-compose crushed-field fix). Plus
**#1327 (S4) closed complete**.

---

## 3. Ready-to-dispatch backlog (issues filed, NOT started)

**Composer overhaul (from the Fable audit on #1616 — read it, it has cited
root-causes + a 5-PR sequence).** Sequence them; PR-1→PR-5 all restructure the
shared `PromptComposerSheet.kt`, so DO NOT run in parallel:

- **PR-1 #1616** — draft-loss (IN FLIGHT, §1 above).
- **PR-2 #1619** — caret-not-visible BUG + offline banner placement/color/wording (M). Next after PR-1.
- **PR-3 #1620** — queue-state banner redesign (S/M, presentation only).
- **PR-4 #1621** — send-while-sending pipelining (M, behavioral — watch the #961 dedupe).
- **PR-5 #1622** — sheet chrome polish (S, cosmetic).
- Maintainer wants the composer **convenient + predictable**; implement PRs,
  show renders (`scripts/render.sh`) for the design choices so they can
  course-correct. #1613 already fixed the reconnect-banner crush (pending next
  release).

**Usage:**

- **#1618** — add the relative "in N hours/days" countdown to the quota
  NOTIFICATION reset time (currently absolute-only). Small; reuse the #1565
  `formatResetAbsolute` + the existing relative helper.

**Test reliability (test-infra, no product change):**

- **#1617** — payload-variant `TmuxSessionAgentSendTest` flake root: the #1168
  `agentTailScope`/`factoryScope` accumulate real-IO across tests (fires at 50×;
  the #1584/#1599 CI red). Fix after #1615 lands.

---

## 4. Awaiting MAINTAINER decision (do NOT proceed without it)

- **#1610 Step 2 — the mobile ~1/min reconnect ROOT.** Diagnosed from the
  maintainer's real host-mirrored cause-trail (`~/.pocketshell/connection-log.jsonl`,
  the #972 mirror — READ IT for evidence): the tmux **-CC control-channel reader
  throws `SSHException`** and tears down the shared lease transport
  (`ExplicitDisconnect stage=lease_transport`), plus the 60s `IdleExpired`
  reaping idle sessions. A D31 recurrence of #1562 (still live on v0.4.37)
  despite #1567/#1568. Step 1 (zero-leak exception classification) is MERGED —
  the next mobile session will log the exact exception shape
  (`broken_pipe`/`connection_reset`/`eof`/…). **Step 2 (tolerate transient
  control-channel read errors + stop `IdleExpired` reaping open sessions) is the
  maintainer's rewrite-vs-patch call** — they steered "rewrite over patch"
  generally, so it likely folds into the S7 consolidation, but they haven't
  confirmed for this specific fix.
- **S7 (#766) — the connection old-path DELETION.** The #1331 consolidation is
  substantially done (S0–S6, incl. S4/#1327 closed). S7 deletes the old inline
  `TmuxSessionViewModel` reconnect/grace path (making `ConnectionController` the
  sole path — the D28 "done"). It is EXPLICITLY gated on **maintainer on-device
  sign-off** (#766 DoD: bg→fg no-reconnect #635, no blank-after-reconnect #553,
  correct-session-on-switch #686/#658, Codex burst-redraw #576). Do NOT delete
  before that sign-off. S8 (#1330, coordinator extraction) depends on S7.
- **#1538 — within-grace foreground-heal flap.** DEFERRED, inconclusive: the
  auto-generated reproduction hit the #1110 real-`Dispatchers.IO` virtual-clock
  trap (harness timeout; load-bearing assertion never ran). Re-attempt AFTER
  #1615/#1617 land the deterministic test seam (the #1110 fix makes a #1538
  repro runnable). Test preserved in `.claude/worktrees/agent-a2557608f8aa43cd4`.

---

## 5. Key context / gotchas

- **The maintainer's device connection-log mirrors to this host** at
  `~/.pocketshell/connection-log.jsonl` (#972). Parse it (JSONL:
  category/name/metadata; `category=reconnect name=cause_trail`) to see real
  reconnect causes — invaluable for connection bugs.
- **quse is a sibling repo** at `~/git/quse` (published to PyPI via tag `v*` →
  `release.yml`). 0.0.11 shipped this session. Dev-box `uv` has a rolling
  `exclude-newer` that blocks same-day resolves — use `uv run --frozen` or the
  committed lock.
- **Recurring flake:** `TmuxSessionAgentSendTest …ReconnectsAndSendsWhenDisconnectedRecoverable`
  (both variants) flakes on unrelated PRs' `Unit tests` (#1110/#1168
  real-dispatcher coupling). If a PR's Unit job reds on it and the PR touched no
  connection code, it's this flake — classify (G5), rerun the failed job. Being
  fixed via #1615 (agent variant) + #1617 (payload variant).
- **`gh issue create` does NOT support `--json`**; capture the URL from stdout.
  **Backticks in a double-quoted `--body` trigger shell substitution** — use
  `--body-file`.
- **Screenshots** land in `~/.pocketshell/attachments/host-1-git-pocketshell/`;
  attach to issues via the `feedback-assets` prerelease (never commit), then
  delete from inbox (the screenshot-to-issue skill).
- Process reminders: orchestrator stays on synced `main`, never switches the
  root branch; per-issue worktrees; on-call watches CI; hygiene guards
  (`check-file-size-hygiene.sh`, `check-connection-vm-ratchet.sh`) are NOT Gradle
  tests and must be run before pushing VM-touching changes; run the FULL
  `:app:testDebugUnitTest` for connection/composer changes (narrow `--tests`
  masked a sibling regression this session).

---

## 6. Suggested next-actions checklist (incoming orchestrator)

1. Check the two in-flight agents (#1615, #1616-PR-1); review → gate → merge as
   they report.
2. Sequence the composer PRs: PR-1 (in flight) → PR-2 #1619 → PR-3 #1620 → PR-4
   #1621 → PR-5 #1622. Show renders for design PRs.
3. Dispatch #1618 (usage-notif relative countdown) — independent, safe to
   parallelize with a composer PR (different files: usage vs composer).
4. After #1615 merges, dispatch #1617 (payload flake), then re-attempt #1538.
5. Surface the two maintainer decisions (#1610 Step 2, S7 #766) and hold them
   until answered.
6. Keep `main` green; batch heavy CI; don't saturate the 20-job Actions budget.
