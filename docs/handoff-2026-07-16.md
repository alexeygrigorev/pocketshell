# Orchestrator session handoff — 2026-07-16

`main` is clean and synced with `origin/main` at **3071ae2f**. There are **0
open PRs**. Read `process.md` first; this document is the live continuation
state. The active backlog is not finished.

## 1. Stop state

All built-in subagents were stopped or had already completed when this handoff
was written. In particular, `/root/issue_1619_medium`, the #1621 round-three
implementer, was interrupted at the maintainer's request. Its worktree was
preserved exactly as-is; do not reset, clean, recreate, or discard it.

The launcher continued to force `gpt-5.6-sol` **Medium** despite the configured
High effort. The maintainer explicitly authorized Medium built-in Codex
subagents for this active backlog run. This exception does not waive worktree
isolation, the implementer → independent-reviewer loop, or verification gates.
For a new run, verify the actual child model/effort before editing; use built-in
subagents, not shell-launched substitutes.

## 2. Merged during this continuation

| Issue | PR | Result | Merge commit | Required CI |
|---|---:|---|---|---|
| #1615 | #1624 | Deterministic agent-variant teardown test fix; issue closed | `a7bce06f` | Run 29451479608 green |
| #1616 PR-1 | #1625 | Revision-safe composer draft preservation | `1df16f03` | Run 29453068760 green |
| #1618 | #1626 | Relative quota-reset notification countdown; issue closed | `b187ff25` | Run 29454659797 green |
| #1617 | #1627 | Payload test-flake isolation; issue closed | `ba4d5df8` | Run 29455956110 green |
| #1619 | #1628 | Long-draft caret/offline layout; issue closed | `3eeea505` | Run 29458851596 green |
| #1538 | #1629 | Existing product fix proved by two regression cases; issue closed | `82d84916` | Run 29460102065 green |
| #1620 | #1630 | Queue-state presentation redesign; issue closed | `3071ae2f` | Unit and Python jobs green |

#1616 remains open as the umbrella for the remaining composer sequence. A
temporary agent-capacity failure during #1620 integration was recovered; #1620
is fully merged and is not an active problem.

## 3. In-flight #1621 — PR-4 send pipelining

- Issue: [#1621](https://github.com/alexeygrigorev/pocketshell/issues/1621)
- Durable brief: [implementation contract](https://github.com/alexeygrigorev/pocketshell/issues/1621#issuecomment-4986012552)
- Worktree: `.worktrees/issue-1621`
- Branch: `issue-1621-implementer`
- Base and current HEAD: `3071ae2f2aea09b90c56b8d81dd98a12985895be`
- State: **uncommitted, unpushed, no PR, no approval**

The stopped worktree contains ten tracked modifications and two untracked test
files:

- `ComposerDraftDurabilityE2eTest.kt`
- `PromptComposerDraftLossOnFinalizeE2eTest.kt`
- `PromptComposerOutboundSend.kt`
- `PromptComposerSheet.kt`
- `PromptComposerViewModel.kt`
- `PromptComposerWatchdogs.kt`
- `PromptComposerAttachmentWedgeTest.kt`
- `PromptComposerOutboundQueueClogTest.kt`
- `PromptComposerOutboundSendQueueViewModelTest.kt`
- `scripts/ci-journey-suite.sh`
- untracked `PromptComposerSendPipeliningE2eTest.kt`
- untracked `PromptComposerSendPipeliningTest.kt`

The tracked diff is currently 420 insertions and 223 deletions. `git
diff --check` is clean. The untracked tests are not included in that count.

### Review history and current design

- [Initial implementer report](https://github.com/alexeygrigorev/pocketshell/issues/1621#issuecomment-4989960301)
- [Round-one changes requested](https://github.com/alexeygrigorev/pocketshell/issues/1621#issuecomment-4990025355)
- [Round-two implementer report](https://github.com/alexeygrigorev/pocketshell/issues/1621#issuecomment-4990548238)
- [Round-two changes requested](https://github.com/alexeygrigorev/pocketshell/issues/1621#issuecomment-4990634038)

The implementation separates outbound handoff from wire delivery: distinct
ownership and watchdogs, enqueue-only queue commits, drain-owned upload and
delivery, revision-safe clearing, FIFO progression, and per-row transient close
entitlement. It also adds same-`sendKey` retry coalescing, composer
open/mutation epochs, and attachment-job identity checks.

Round two still reproduced two blockers: a Docker duplicate-repaste/auto-flush
race, and a B handoff timeout that could cancel C's newer attachment job. Round
three added deterministic red tests and fixes for:

1. Same-active-key retry consuming exactly the acknowledged retyped revision.
2. Slow B commit/timeout preserving C's newer attachment job.
3. An older queued auto-flush completion not closing a newly mounted composer.

Those three focused race tests were reported green. Three consecutive isolated
Docker composer runs were also reported green, with exact input-empty and
once-only assertions. Earlier round-two evidence included an 18-case JVM
matrix, 24 connected cases, the full JVM suite, Android compilation, and static
guards. However, the implementer was interrupted while rerunning the final
round-three full/static gates and did not post a round-three completion comment.
Do not treat the work as reviewed or complete.

### Exact restart for #1621

1. Inspect the preserved worktree with `git status`, `git diff`, and the
   untracked tests. Do not reset it.
2. Give a Medium built-in implementer the same worktree and ask it to finish
   the round-three full/static gate rerun, resolve any failures without weakening
   assertions, and post an issue completion comment.
3. Assign a different Medium built-in reviewer. It must independently rerun the
   three deterministic races, the Docker stability proof (three consecutive
   isolated runs), the locked JVM/connected matrix, full JVM suite, compile and
   all required size/ratchet/static guards.
4. Only after approval, integrate through a clean integration worktree, push a
   PR, watch required CI, merge, close #1621, and sync root `main`.

## 4. Next composer slice — #1622

#1622 must start only after #1621 merges. Its durable brief is
[here](https://github.com/alexeygrigorev/pocketshell/issues/1622#issuecomment-4986094597).
The diagnosed keyboard-up dead band is the extra `safeDrawing.Top` inset before
the Material 48dp handle, not the partial anchor. Omit only the top inset while
the keyboard is up; preserve partial-expand semantics. Use the established
small loading spinner and a stable minimum-width status pill. This slice has
not started.

## 5. Codex-session reconnect/black-screen incident

The maintainer's general Internet connection is healthy and Termius works, but
PocketShell sessions running Codex repeatedly show a black screen and
reconnect/Attaching cycles. The latest mirrored log contains no repeated
network callbacks and no `pane_input_send` storm. Instead, a fresh #1610/#1562
control-reader exception/read failure tears down the shared lease, followed by
`ExplicitDisconnect` roughly every 5–7 seconds with the attempt counter stuck
at 1. This explains why other apps and an ordinary SSH client remain healthy:
they do not exercise PocketShell's tmux `-CC` control-channel lifecycle.

Evidence and tracking:

- [Primary #1610 incident comment](https://github.com/alexeygrigorev/pocketshell/issues/1610#issuecomment-4989310817)
- [#1610 decision brief](https://github.com/alexeygrigorev/pocketshell/issues/1610#issuecomment-4986673974)
- [#1562 recurrence/override](https://github.com/alexeygrigorev/pocketshell/issues/1562#issuecomment-4989318062)
- [#1604 cross-link](https://github.com/alexeygrigorev/pocketshell/issues/1604#issuecomment-4989323384)
- [Green/black screenshot](https://github.com/alexeygrigorev/pocketshell/releases/download/feedback-assets/issue-1610-20260716-green-black.png)
- [Reconnecting/Attaching screenshot](https://github.com/alexeygrigorev/pocketshell/releases/download/feedback-assets/issue-1610-20260716-reconnecting-attaching.png)

The green dot during Attaching matches the current UI mapping; it is confusing
but not evidence of a healthy control channel. #553 is not proved by this
sample because no successful reseed occurred.

Immediate workaround: copy unsent text, force-stop PocketShell without clearing
storage/cache, relaunch once, and open the session once. If black/Attaching
returns, use Termius and stop repeated PocketShell retries.

No incident fix is authorized yet. The pending maintainer decision is a
two-stage hybrid:

1. Narrow exact-client pane-input quarantine for the earlier 14-event input
   storm. This is still valid containment but would not fix the latest sample.
2. Authorize the urgent S7 single-recovery-authority rewrite for the current
   control-reader/shared-lease loop.

Do not implement either implicitly. Ask for an explicit decision before
starting #1610/S7 work.

## 6. Preserved worktrees and incoming checklist

`.worktrees/issue-1621` is the only current unfinished product worktree and must
be preserved. Older source/recovery worktrees remain on disk, including
`.worktrees/issue-1538`, `.worktrees/issue-1617`, `.worktrees/issue-1620`, and
several `.claude/worktrees/*`; their presence does not mean their already-merged
issues are active. Do not bulk-clean worktrees during recovery.

Incoming orchestrator order:

1. Read `process.md` and this handoff.
2. Resume and finish #1621 from its preserved diff through independent review,
   PR, required CI, and merge.
3. Start #1622 only after #1621 is on `main`.
4. Ask the maintainer for an explicit #1610 containment + urgent S7 decision.
5. Keep root on clean, synced `main`; preserve the subagent and review rules.
