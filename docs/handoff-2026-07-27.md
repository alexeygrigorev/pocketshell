# Orchestrator handoff — 2026-07-27 23:30 Europe/Berlin

This document records the exact state when the maintainer said:

> create handoff document for what you do. stop everything

Do not resume backlog execution merely because this file exists. Resume only
after a new explicit maintainer request.

Read `AGENTS.md` and `process.md` completely before touching any candidate.
`CLAUDE.md` intentionally contains only `@AGENTS.md`.

## 1. Stop state

All task-owned work was stopped before writing this handoff:

- `/root/issue_1741_current` — interrupted
- `/root/issue_1598_current` — interrupted
- no CI watcher agent remains active
- detached #1741 JVM service/scope stopped:
  - `pocketshell-i1741-full-jvm-detached.service`
  - `pocketshell-full-jvm-3418550.scope`
- stale #1598 JVM scope stopped:
  - `pocketshell-full-jvm-3410287.scope`
- no `watch-ci.py`, #1741, #1598, or `pocketshell-full-jvm` process remained
  after the stop

The pre-existing shared Android emulator and tmux sessions were deliberately
left running; they were not owned by this stop operation.

GitHub Actions run
[30305256109](https://github.com/alexeygrigorev/pocketshell/actions/runs/30305256109)
continues server-side. It was not cancelled. Nothing locally is watching it.

## 2. Repository and integration state

Root checkout:

```text
/home/alexey/git/pocketshell
HEAD        aad99d46560bccaf152d2b90d5dea681fc04057c
origin/main aad99d46560bccaf152d2b90d5dea681fc04057c
status      clean before this handoff file was added
```

Only open PR at handoff time:

- draft [#1711](https://github.com/alexeygrigorev/pocketshell/pull/1711),
  `issue-1487`

There is no active integration PR. Keep the process invariant of one meaningful
integration PR at a time.

### Recently integrated wave

| Issue(s) | PR | Main result |
|---|---|---|
| #1789 | #1792 | Codex reset-credit inventory/expiry merged |
| #1768 | #1793 | merged and exact-main validated |
| #887 + #1748 | #1794 | merged and exact-main validated |
| #1714 | #1795 | merged |
| #1754 | #1796 | merged |
| #1733 | #1797 | merged as `343dac95`; exact-main green |
| #1739 recurrence | #1798 | merged as `aad99d46`; exact-main run still in progress |

### #1733 final evidence

Exact-main run
[30298932001](https://github.com/alexeygrigorev/pocketshell/actions/runs/30298932001)
tested exact SHA `343dac95c1965c7f61dc627d4720bbaaed12ff45`
and completed successfully:

- Unit tests
- Python utility tests
- Integration tests (Docker)
- Emulator journey subset

Evidence was recorded on
[#1733](https://github.com/alexeygrigorev/pocketshell/issues/1733#issuecomment-5096699428).

The clean merged #1733 integration worktree/branch was removed. An exact merged
candidate worktree was removed only after every changed/untracked file was
proved byte-identical to main. Older divergent #1733 research worktrees remain
preserved and must not be force-removed without a separate evidence audit.

### #1739 recurrence final state

Issue [#1739](https://github.com/alexeygrigorev/pocketshell/issues/1739) is
closed. PR [#1798](https://github.com/alexeygrigorev/pocketshell/pull/1798)
merged as:

```text
main commit  aad99d46560bccaf152d2b90d5dea681fc04057c
PR head     533d692c1893db64428cdc062549f47092fac266
tree        dee8c02ba0f1b6fbb3abed9d46fca95c89f477c2
```

The PR head and squash-merged main trees are identical. PR CI run `30303096020`
was green for the two required checks:

- Unit tests
- Python utility tests (pocketshell)

Exact-main run `30305256109` has exact `headSha=aad99d46...`. At the stop-time
snapshot it was `in_progress`. The delegated `scripts/watch-ci.py` invocation
had been externally SIGTERM'd after about seven minutes, when one job was green
and two were running. It emitted no failure signature and no final JSON.

Do not invoke `scripts/watch-ci.py` a second time for this push: the exactly-once
watcher invocation already happened. On resume, make one terminal metadata
lookup for run `30305256109` and verify:

```text
headSha    aad99d46560bccaf152d2b90d5dea681fc04057c
status     completed
conclusion success
```

If green, record exact-main evidence on #1739, then remove only the clean merged
worktree/branch:

```text
.worktrees/issue-1739-recurrence-integrate-343dac95
issue-1739-recurrence-integrate-343dac95
```

The older proof worktree
`.worktrees/issue-1739-recurrence-f68a157` contains preserved mutation/dynamic
evidence and should not be casually removed.

## 3. #1662 — technically approved, waiting on maintainer visual signoff

Issue:
[#1662](https://github.com/alexeygrigorev/pocketshell/issues/1662)

Authoritative worktree:

```text
/home/alexey/git/pocketshell/.worktrees/issue-1662-343dac95
base 343dac95c1965c7f61dc627d4720bbaaed12ff45
candidate patch SHA-256
a3d3b09c4255dafcad84cecdf8379419138a868ca3d9331180d6aac4e5fc7a17
scope 26 paths
```

Current main differs from that base only by the unrelated #1739 test-harness
file, but a final integration candidate must still be reconciled onto exact
current main.

Technical state:

- independent review: `APPROVED`
- forced assemble/Android-test compilation: 272/272 tasks
- canonical JVM gate: 603 tasks, 11,517 tests, zero failures/errors/skips
- real connected run: 1/1, zero failures/errors/skips
- exact emitted bytes:
  `11 1c 02 02 03 03 04 04 03 03 04 04`
- Back, close, and keyboard-up Back each emitted zero control bytes
- ordinary C/C/D/D taps each recorded `repeat=1`
- 48dp, narrow/large-font, accessibility, disabled state, and mutation coverage
  independently approved

Fresh screenshots awaiting maintainer approval:

- [main hotkeys sheet](/home/alexey/pocketshell-preservation/issue-1662-343dac95-reconciliation-20260727T193952Z/connected-final-artifacts/issue784-06-hotkeys-panel-fulldevice.png)
- [Ctrl page](/home/alexey/pocketshell-preservation/issue-1662-343dac95-reconciliation-20260727T193952Z/connected-final-artifacts/issue1662-ctrl-page-fulldevice.png)

Do not close or integrate #1662 without an explicit maintainer visual approval.

## 4. #1741 — exact-current candidate, stopped before the canonical JVM gate

Issue:
[#1741](https://github.com/alexeygrigorev/pocketshell/issues/1741)

Authoritative stopped worktree:

```text
/home/alexey/git/pocketshell/.worktrees/issue-1741-aad99d46
base aad99d46560bccaf152d2b90d5dea681fc04057c
branch issue-1741-aad99d46
commit/push/PR none
```

Current scope:

- modified `.github/workflows/tests.yml`
- modified
  `app/src/androidTest/java/com/pocketshell/app/notifications/NoNotificationPromptOnAppOpenE2eTest.kt`
- modified `scripts/ci-journey-budget-functions.sh`
- modified `scripts/connected-test.sh`
- modified `scripts/nightly-extensive-suite.sh`
- modified `scripts/test-ci-journey-budget.sh`
- untracked `scripts/test-notification-permission-fixture.sh`

Aggregate stopped-tree patch hash, including the untracked fixture:

```text
be269ab1e61d7513292445736fa89b6979b1a703ee39e4bc557259d245ac82fb
```

It was computed with:

```bash
{
  git diff --binary
  git diff --no-index --binary /dev/null \
    scripts/test-notification-permission-fixture.sh || true
} | sha256sum
```

Implemented behavior:

- exact package/suffix is installed before instrumentation
- notification permission is revoked and hard-verified externally
- instrumentation starts only after the denied state is established
- permission restoration occurs after instrumentation on success/failure
- primary and cleanup failures remain distinguishable and red
- the Kotlin test hard-asserts denied permission and never revokes/grants from
  its own instrumentation process
- both serial and sharded per-push routes select the canonical fixture
- nightly phase 1b runs the class once on shard 0, preserves its own report and
  count, affects human extensive status, and stays outside `fault-verdict`

Reported evidence before stop:

- fixture mutation matrix covered revoke failure, state drift, unsupported API,
  missing package, runner kill/no report, zero tests, named-test failure,
  restore failure, serial/sharded routing, and nightly verdict separation
- three consecutive API-35 wrapper runs passed 1/1
- the original app-open notification-prompt behavior was reintroduced as a
  mutation; the fixed test stayed alive and failed its product/focus oracle;
  restoration returned it green
- fixture, harness, validity, shell syntax, and diff hygiene were reported green

Important evidence caveat:

- the current exact-aad evidence was not sealed into a preservation directory
  before the stop
- similarly named `/tmp/issue1741-*` files include older July 26 evidence and
  must not be mistaken for exact-aad proof without checking timestamps/content
- locate and preserve fresh connected XML/logcat/focus artifacts, or rerun them
  if provenance cannot be proved

The canonical no-argument JVM gate is **not green evidence**. An earlier run was
mistakenly called green based on a systemd scope result, then correctly
retracted because the child was still mid-suite. The replacement exact-aad run
was deliberately stopped at the maintainer's request:

```text
log /tmp/pocketshell-issue-1741-aad-full-jvm.log
rc  absent
```

The log ended mid-test graph; it is neither green nor a product failure.

Required next work after an explicit resume:

1. Preserve or repeat exact-aad connected RED/GREEN/mutation evidence with
   nonzero XML counts.
2. Run the canonical no-argument `scripts/full-jvm-gate.sh` to a durable child
   exit and fresh totals in a quiet local lane.
3. Re-run final forced Android-test compile and current static/harness/budget
   gates if their exact-aad provenance cannot be sealed.
4. Compute and record a final patch hash including the untracked fixture.
5. Obtain an independent no-edit review.
6. Only then commit/push/open the single integration PR.

## 5. #1598 — exact-current implementation, stopped before full/connected gates

Issue:
[#1598](https://github.com/alexeygrigorev/pocketshell/issues/1598)

The issue body was reconciled during this run. It now describes only the actual
residual:

- carry authoritative `holdActive` through `ACTION_START`
- emit `hold_active` on promotion success and denial
- use the real producer key `error`
- remove tests that fabricate `exceptionClass`
- preserve #1757 lifecycle behavior and all mirror/action routing

Authoritative stopped worktree:

```text
/home/alexey/git/pocketshell/.worktrees/issue-1598-aad99d46
base aad99d46560bccaf152d2b90d5dea681fc04057c
branch issue-1598-aad99d46
commit/push/PR none
patch SHA-256 26690fcdf5410035658fd4555b717507417c754bdee557fe2c7a10e471115dbf
```

Three modified files:

- `app/src/main/java/com/pocketshell/app/sessions/service/SessionConnectionService.kt`
- `app/src/test/java/com/pocketshell/app/diagnostics/Issue1642ConnectionMirrorTest.kt`
- `app/src/test/java/com/pocketshell/app/sessions/service/SessionConnectionServiceFgsDiagnosticsTest.kt`

Verified implementation evidence:

- exact base RED: 15 tests executed, exactly 3 failures,
  `No value for hold_active`
- fixed GREEN: 15/15
- real request-denial producer mirror proof uses `error`, asserts
  `hold_active`, and asserts no fabricated `exceptionClass`
- mutations killed:
  - drop ACTION_START extra
  - hard-code true
  - omit promotion-success field
  - omit promotion-denied field
  - rename promotion `error`
  - rename request `error`
- `action/shortcut_sent` remains device-only
- focused exact-aad JVM log: `BUILD SUCCESSFUL`
- forced Android-test compile log: `BUILD SUCCESSFUL`
- validity and journey-harness guards: green

Preserved logs:

```text
/home/alexey/pocketshell-preservation/issue-1598-aad99d46-20260727T2120Z/
  focused-jvm.log
  androidtest-compile.log
  hygiene.log
  full-jvm-gate.log
```

`full-jvm-gate.log` is incomplete. The run was externally terminated at
`:app:compileDebugKotlin`; its later stale scope was stopped during the explicit
stop operation. It provides no green or failure verdict.

The existing connected host-mirror journey had not been run on this candidate
before the stop.

Required next work after an explicit resume:

1. Run canonical no-argument full JVM in a quiet lane to a durable exit/totals.
2. Run the already-wired connected host-mirror journey and verify exact remote
   JSON keys/bytes with nonzero executed counts.
3. Seal final patch/hash and artifacts.
4. Obtain independent no-edit review.
5. Refresh after any earlier wrapper/main merge, then integrate one PR at a
   time.

## 6. #1742 — prepared, but wait until #1741 merges

Issue:
[#1742](https://github.com/alexeygrigorev/pocketshell/issues/1742)

Verdict: `WAIT for #1741, then GO immediately`.

Best existing candidate:

```text
.worktrees/issue-1742-current-main-pickup
base 8f7167f4e698e2138ca5f491de587e3acb2eb06b
scope three tracked files
patch SHA-256 d9b5dacf924d647c3e4f7a7a9cc595bb70ce41cf22f21f32a6d27fb50c8eda0b
```

It applies cleanly to `aad99d46`, but has no preserved dynamic/mutation evidence.
#1741 owns the wrapper/report semantics required to prove #1742, so do not
dispatch #1742 until #1741 is merged and exact-main settled.

Afterward, recreate it from exact main, preserve every newer journey registry
entry, and prove:

- original real-IME precondition RED
- activity-only synthetic dispatch root-cause RED
- all-app-root synthetic dispatch GREEN
- exact modal inset and owning-root identity
- real `worksh -> workshops` filtering
- containment and material lift/shrink
- zero-inset and layout-response mutations RED
- repeated whole-class 1/1 runs with restored AVD/IME state
- forced compile, validity, harness, budget, canonical JVM, and independent
  review

## 7. Other decisions and backlog routing already established

### #1513

Still valid, but HOLD for:

1. #1741 workflow overlap, and
2. one historical maintainer decision.

The question is whether the literal in-app Hetzner Update succeeded after #1490.
If that cannot be confirmed, the maintainer must explicitly waive that
historical proof before the stale-uv-index defense-in-depth fixture is built.

### Rework queues

- #1778: immutable scroll owner must reach the VM action; numeric page index is
  not sufficient.
- #1751: terminal-echo oracle is false; exact client/session diagnostics and
  classifier need rework; requires 20 engaged runs.
- #1765: inventory is 28 files / 29 constructors / 169 tests; stale overlap
  list and real VM child teardown proof require rework.

### Sequenced product work

- #1622 after #1662 visual signoff; final visual signoff required.
- #1688 after #1662 + #1622; it is the single outbound-worker hard cut.
- #1563 after #1622 + #1688.
- #1576 is narrowed to R3 only; GO after active VM lanes.
- #1634 is GO after active lanes: stale-session recreation first, then bounded
  non-cancellable connect.
- #1598 is the active exact-current diagnostic candidate described above.

### Human/policy decisions still open

- #1662 visual approval: screenshots listed above.
- #1700: recommended durable `HeldForReview` after five minutes; never
  auto-drop.
- #1752: recommended refresh on attach/switch/restore/reclassification/app
  foreground; no while-idle polling.
- #1753: recommended persist after placed + next frame, no timer, any enabled
  IME-hidden launcher, custom accessibility dictation action.
- #1715: recommended session-menu `Open files` route.
- #1487: duplicate kebab status removal and Android Live Update tradeoff need
  maintainer approval; API-36 real-device proof also remains.
- #1706: parked for human key-custody/uninstall decisions.

## 8. Safe resume order

Only after a new explicit request:

1. Read `AGENTS.md`, `process.md`, and this handoff.
2. Confirm root is clean/synced and inspect all listed worktrees before edits.
3. Make the one terminal exact-SHA lookup for run `30305256109`; do not start a
   second #1739 watcher.
4. If #1662 visual approval arrived, reconcile it to exact main but keep one
   integration PR at a time.
5. Finish #1741's missing exact-aad preservation/full-JVM/review; it is closest
   to complete and unlocks #1742.
6. Finish #1598 full-JVM/connected/review after refreshing through any #1741
   wrapper merge.
7. Integrate reviewed candidates one at a time, with exactly one delegated
   watcher per push and exact-main semantic validation before cleanup.
8. Start #1742 only after #1741 merges.

Do not infer green from a systemd unit's `Result=success`; that can describe the
unit wrapper even when a child was killed or still running. Require the actual
child exit, final build verdict, fresh test totals, and nonzero executed counts.
