#!/bin/sh
set -eu

fixture_dir="${POCKETSHELL_AGENT_FIXTURE_DIR:-/opt/pocketshell-agent-fixtures}"

if [ "${1:-}" = "usage" ] && [ "${2:-}" = "--json" ]; then
  cat "${fixture_dir}/heru-usage.json"
  exit 0
fi

printf 'heru fixture supports: usage --json\n' >&2
exit 64
