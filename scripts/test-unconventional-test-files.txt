# Reviewed exemptions: @Test-bearing files outside the *Test.kt / *Test.java
# naming convention (issue #2065, mechanism from #2063).
#
# WHY THIS FILE EXISTS
#
# Every registry, guard and Gradle filter in this repo keys off the *Test.kt /
# *Test.java convention: the journey registry (scripts/ci-app2-journey-suite.sh), the
# area manifest (scripts/test-areas.txt via pocketshell_test_areas_fqcn_for_path),
# the executed-classes ledger (scripts/check-test-execution-ledger.sh) and the
# validity guards. A @Test-bearing file that does not follow it is invisible to
# all of them — the #1851 shape, and worse than a missing test because it looks
# like coverage in the tree and in review.
#
# #2063 baselined the offenders inside select-test-areas.sh as a flat array of
# paths, so a NEW one fails. #2065 is the other half: each existing file gets a
# decision, and the decision is recorded here in a form the guard can CHECK
# rather than a comment it has to be trusted about.
#
# IT IS DELIBERATELY NOT A `*Harness.kt` SUFFIX RULE. A suffix would be a
# self-serve escape hatch — name a real regression test `…Harness.kt` and it
# hides itself, with no reviewer in the loop. A per-file row with a machine-
# checked justification keeps a human decision in the path.
#
# `*Journey.kt` IS part of the convention, and is NOT exempted here. app2's
# numbered instrumented journeys (J01ConnectAndTrustJourney, …) carry the
# load-bearing assertion for their user journey, which by this file's own rule
# means the answer is "be a recognised test class", not "be exempted". They are
# recognised by pocketshell_test_areas_is_test_class_file, so the class index,
# the area manifest, the ledger and Gradle `--tests` can all see them.
#
# FORMAT — four TAB-separated fields, one row per file. `#` comments and blank
# lines are ignored.
#
#   <path>  <executor>  <gate>  <reason>
#
# <path>     repo-relative. Must be tracked, live in a test source set, carry
#            @Test, and NOT match the naming convention — otherwise the row is
#            stale and the guard fails (an exemption that has outlived its file
#            is how a baseline rots into a lie).
#
# <executor> HOW the file's @Test methods actually reach a runner. Answering
#            "does it execute in any gate?" per file is the whole point of
#            #2065; a claim is only allowed if the guard can confirm its shape.
#              unit-source-set    the path is under */src/test/ AND is a
#                                 compiling Kotlin/Java source (*.kt / *.java),
#                                 so `./gradlew test` — the required `Unit
#                                 tests` check — runs it unconditionally. A
#                                 parked non-source such as *.kt.txt is
#                                 rejected (#2170): the directory prefix alone
#                                 is not an execution claim.
#
#            `unit-source-set` is the ONLY executor kind. The former
#            `nightly-connected` kind died with the rewrite's hard cut (D22): it
#            rested on nightly phase 1 running `:app:connectedDebugAndroidTest`
#            WHOLESALE minus a `notClass` list, and neither the module nor that
#            shape exists any more (app2's lane runs its whole instrumented set
#            unfiltered in ONE process, with no exclusion list to be absent
#            from). An instrumented file that needs an exemption today has no
#            checkable execution claim, so it does not get one — name it to the
#            convention instead.
#
# <gate>     WHAT accounts for the file, given no registry can see it.
#              <FQCN>                 a conventional test class that carries the
#                                     load-bearing assertion for the same
#                                     behaviour. The guard resolves it through
#                                     the SAME class index the area manifest and
#                                     the ledger use, so the gate is provably a
#                                     registered, area-resolved, ledger-visible
#                                     class — not just a name that reads well.
#              enumerated-by:<script> a purpose-built registry that enumerates
#                                     THIS file's @Test methods. The guard
#                                     asserts the script exists and references
#                                     the path.
#
# <reason>   free text, required. Why renaming is the wrong answer for this
#            file.
#
# ADDING A ROW IS NOT A FORMALITY. If the file's @Test methods carry the
# load-bearing assertion for anything, the answer is to RENAME it, not to list
# it here.
#
shared/ui-kit/src/test/java/com/pocketshell/uikit/render/DesignRenders.kt	unit-source-set	enumerated-by:scripts/render.sh	Fast design-render harness (#555). Observed executing 68 testcases, 0 skipped, in :shared:ui-kit:testDebugUnitTest, so it is not silently dead. It is also the ONE file here with a stronger registry than the ledger could give it: scripts/render.sh parses every @Test method out of this source and hard-fails when one has no `fun m() = render("label")` mapping — per-METHOD accounting, pinned per push by scripts/render-selftest.sh in the guards-static job ("missing test-to-label mapping is rejected"). The module's execution is separately ledger-observed through the sibling conventional DesignRenderStaticLoadingPolicyTest in this same package. Renaming would break render.sh's RENDER_CLASS/RENDER_SOURCE constants, ~10 render-selftest.sh fixtures and four docs that name the file as the design-iteration entry point, and buy no accounting that is not already there.
app2/src/test/java/com/pocketshell/next/render/ComposerRenders.kt	unit-source-set	com.pocketshell.next.composer.ComposerBarTest	Roborazzi design-render harness for the composer (task P-1), the app2 sibling of :shared:ui-kit's DesignRenders. It renders composer states to PNGs under app2/build/renders/ for the maintainer to look at; the file says so itself ("These are renders, not assertions"). Every state it draws is behaviourally asserted by the gate class, and the real acceptance for composer geometry is the emulator journey J07ComposerSendJourney against the actual keyboard. Renaming it to *Test.kt would put a deliberately assertion-free picture-producer into every registry as though it were a gate — the exact "looks like coverage, contributes nothing" deception #2065 exists to end. It executes on every push: it is a compiling Kotlin source under app2/src/test/, so `./gradlew test` runs it unconditionally, which is also what keeps the renders from silently rotting.
app2/src/test/java/com/pocketshell/next/render/HostScreenRenders.kt	unit-source-set	com.pocketshell.next.hosts.HostListNavigationTest	Roborazzi design-render harness for app2's host-management screens (task P-6). Same shape and same reason as ComposerRenders above: renders, not assertions — "the behaviour of every screen below is covered by its own test class; nothing here is the only check on anything". It lives in app2 rather than ui-kit because ui-kit is a dependency OF app2 and can never see an app2 composable, so scripts/render.sh cannot grow to enumerate it the way it enumerates DesignRenders. Runs unconditionally in the Unit job as a compiling app2/src/test source.
app2/src/test/java/com/pocketshell/next/render/SessionScreenRenders.kt	unit-source-set	com.pocketshell.next.terminal.SessionScreenTest	Roborazzi design-render harness for the session screen's chrome (tasks U-5/U-7/P-1). It is structurally incapable of being a gate for the thing it pictures: any state that hosts the terminal renders with a BLANK grid on the JVM, because the vendored emulator paints through libtermux.so and its AndroidView takes the whole capture with it. The banner's text is pinned by the gate class and its real appearance by the device screenshot from J05ReconnectAfterDropJourney. Naming a harness with that blind spot *Test.kt would advertise coverage it cannot have. Runs unconditionally in the Unit job as a compiling app2/src/test source.
app2/src/test/java/com/pocketshell/next/render/ShareScreenRenders.kt	unit-source-set	com.pocketshell.next.share.SharePickerScreenTest	Roborazzi design-render harness for the share target (task P-9). Renders, not assertions — the screen's behaviour is covered by the gate class. It exists because the share surface is the one screen the user meets from OUTSIDE the app, with no chrome to explain what happened, so what each state looks like has to be looked at rather than asserted. Runs unconditionally in the Unit job as a compiling app2/src/test source.
app2/src/test/java/com/pocketshell/next/render/PortForwardScreenRenders.kt	unit-source-set	com.pocketshell.next.ports.PortForwardScreenTest	Roborazzi design-render harness for the port-forward screen's connection states (issue #2491), same shape as HostScreenRenders. It exists because a host that has stopped retrying has to look different from one still trying, and "different" is a thing you look at, not a thing an assertion settles — the file's own doc says so ("nothing here is the only check on anything"). The actual behaviour is asserted in PortForwardScreenTest. Runs unconditionally in the Unit job as a compiling app2/src/test source.
app2/src/test/java/com/pocketshell/next/render/SessionTreeScreenRenders.kt	unit-source-set	com.pocketshell.next.tree.SessionTreeScreenTest	Roborazzi design-render harness for the session-tree header after issue #2532 (Back + Usage). Same shape as HostScreenRenders: renders, not assertions — Back/Usage behaviour is asserted in SessionTreeScreenTest. Renaming it to *Test.kt would put a deliberately assertion-free picture-producer into every registry as though it were a gate. Runs unconditionally in the Unit job as a compiling app2/src/test source.
app2/src/test/java/com/pocketshell/next/render/UsageScreenRenders.kt	unit-source-set	com.pocketshell.next.usage.UsageScreenTest	Roborazzi design-render harness for the usage panel's compact-first layout (issue #2534), same shape as HostScreenRenders. Collapsed vs expanded is a thing you look at; tap-to-expand behaviour is asserted in UsageScreenTest. Renaming it to *Test.kt would put a deliberately assertion-free picture-producer into every registry as though it were a gate. Runs unconditionally in the Unit job as a compiling app2/src/test source.
